import { COLOR_TOKENS, CHART_SERIES } from "../../styles/tokens.js";

export const CHART_COLORS = {
  primary: COLOR_TOKENS.actionPrimary,
  success: COLOR_TOKENS.success,
  warning: COLOR_TOKENS.warning,
  danger: COLOR_TOKENS.danger,
  secondary: COLOR_TOKENS.textSecondary,
  series: CHART_SERIES,
};
