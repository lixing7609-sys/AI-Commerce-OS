function List({ title, id, items, render }) { return <article id={id}><span className="sino-kicker">{title}</span>{items?.length ? <ul>{items.map(render)}</ul> : <p>暂无内容</p>}</article>; }

export function SecretarySummary({ digest, onContinue, onConfirm }) {
  return <section className="sino-secretary-summary" aria-label="AI 秘书整理结果"><article className="sino-secretary-summary__main"><span className="sino-kicker">AI 秘书摘要</span><h2>今日讨论</h2><p>{digest?.summary || "讨论开始后，Sino 会在这里持续整理关键信息。"}</p></article><div className="sino-secretary-grid"><List id="decisions" title="已形成决策" items={digest?.decisions} render={(item) => <li key={item.decision_id}><strong>{item.title}</strong><p>{item.content}</p></li>} /><List id="knowledge" title="新增知识" items={digest?.knowledge_items} render={(item) => <li key={item.knowledge_id}><strong>{item.title}</strong></li>} /><List id="candidates" title="候选目标" items={digest?.candidate_goals} render={(item) => <li key={item.goal_id}><strong>{item.title}</strong><p>{item.description}</p>{item.status === "candidate" && <div><button type="button" onClick={() => onContinue(item)}>继续讨论</button><button type="button" onClick={() => onConfirm(item)}>确认为目标</button></div>}</li>} /><List id="questions" title="待确认问题" items={digest?.pending_questions} render={(item) => <li key={item.question_id}><p>{item.content}</p><small>{item.reason}</small></li>} /></div></section>;
}
